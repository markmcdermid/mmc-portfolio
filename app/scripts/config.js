const DEG_TO_RAD =  Math.PI / 180;
const ICON_SIZE = 100;
const CENTRE_X = window.innerWidth / 4 - (ICON_SIZE / 2);
const CENTRE_Y = window.screen.availHeight / 2 - ( ICON_SIZE / 2)

export default {
  DEG_TO_RAD,
  ICON_SIZE,
  CENTRE_X,
  CENTRE_Y
};

